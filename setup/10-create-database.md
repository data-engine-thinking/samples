# Setup - 10 Create Database

> [!NOTE]
> This script drops and recreates the `DataEngineThinking` database.

## Purpose

Create or reset the sample database, set collation and recovery model, create the `PSA` and `LDA` schemas, and create a sample user.

## Run order

Run this script first.

## Script

Use `setup/10-create-database.sql`.

[!code-sql[](10-create-database.sql)]
